# เงื่อนไขจากระบบ SOI8 WEIGHT RECORD → MINISCADA: ฟิลด์ `plant` ของบันทึกการชั่ง

> เอกสารส่งต่อจาก session SOI8MASTER (2026-08-10) — ฝั่ง SOI8 ทำส่วนของตัวเองเสร็จแล้ว
> เอกสารนี้บอก "สิ่งที่ MINISCADA (ซอฟต์แวร์เครื่องชั่งบน Pi / ตัว writer) ต้องทำต่อ"
> เพื่อให้บันทึกการชั่งระบุได้ว่ามาจาก plant ไหน — เดิมทั้งระบบมีแต่ plant Liquid
> ตอนนี้เครื่องชั่ง manual (เข็นย้ายได้: MOBILE01, MOBILE02, 2T01, 2T02) กำลังจะถูกใช้ข้าม plant

---

## สิ่งที่ฝั่ง SOI8 ทำเสร็จแล้ว (ไม่ต้องทำซ้ำ)

1. ตาราง **`[SOI8LOG].[dbo].[kubotalog]` บน 172.23.10.39** เพิ่มคอลัมน์แล้ว:
   ```sql
   plant NVARCHAR(50) NULL
   ```
   (additive-only — คอลัมน์เดิมไม่ขยับ ไม่เปลี่ยนชื่อ ไม่เปลี่ยนลำดับ)
2. แถวเก่าทั้งหมด **100,872 แถว backfill เป็น `'Liquid'` แล้ว** (ยืนยันโดยเจ้าของระบบว่า
   ข้อมูลที่ผ่านมาทั้งหมดเป็น Liquid)
3. ฝั่งอ่าน (SOI8STATUS) ส่ง `plant` ออกใน API ต่อแถวแล้ว — แถวที่ plant เป็น NULL
   จอจะแสดง `-` (ไม่เดา)

## สิ่งที่ MINISCADA ต้องทำ

### 1. เพิ่มคอลัมน์ฝั่ง Pi
ตาราง `kubotalog_backup` (SQLite บน Pi ทุกเครื่องชั่ง) เพิ่ม:
```sql
ALTER TABLE kubotalog_backup ADD COLUMN plant TEXT;
```
(คอลัมน์ปัจจุบัน: `id, ts, station, weig, code, sent, record_id` — ห้ามเปลี่ยนของเดิม)

### 2. stamp `plant` ตอนบันทึกการชั่ง — ทั้งสองที่พร้อมกัน
- แถวใน `kubotalog_backup` (Pi)
- INSERT ไปที่ `kubotalog` บน .39 (เพิ่ม `plant` ใน column list ของ INSERT —
  **ระบุชื่อคอลัมน์ชัดเจนเสมอ** ห้ามพึ่งลำดับคอลัมน์)

### 3. ค่าที่ stamp — กติกาสำคัญ (อัพเดต 2026-08-11 — ชุดค่ากำหนดแล้ว)
- **ชุดค่ามาตรฐาน = 8 key ของระบบ QCFINAL ตัวใหญ่ทั้งหมด สะกดเป๊ะ:**
  `PREMIX` · `COILCOATING` · `HYDROPHILIC` · `PLX` · `TRITRATING` · `POWDER` ·
  `LIQUID` · `NOXRUST`
- ⚠️ เปลี่ยนจากเวอร์ชันก่อนของเอกสารนี้: เดิมบอก `Liquid` — ตอนนี้ข้อมูลเก่าทั้งหมด
  ถูกปรับเป็น `LIQUID` แล้ว (100,872 แถว, 2026-08-11) ใช้ตัวใหญ่เท่านั้น
- **แหล่งค่าที่ถูกต้อง ณ เวลาชั่ง**: ถาม SOI8STATUS —
  `GET http://172.23.10.34:18020/stationsetting` → `data.stations[<STATION>].plant`
  (เจ้าของระบบตั้ง/ย้าย plant ของแต่ละเครื่องจากหน้า PC web; เครื่องที่ล็อกอยู่ค่าไม่ขยับ)
- **ไม่รู้ plant / ถาม endpoint ไม่ได้ = เขียน NULL** ห้าม default ในโค้ด writer —
  ฝั่ง SOI8STATUS มี **plant stamper** เติมแถว NULL ให้เองทุก 30 วินาที
  (หน้าต่างย้อนหลัง 6 ชม.) ตามตำแหน่งเครื่องปัจจุบัน — งาน stamp ฝั่ง MINISCADA
  จึงเป็น "ทำได้ยิ่งดี" ไม่ใช่ตัวบล็อกระบบอีกต่อไป แต่ถ้า stamp เองต้องใช้ค่าจาก
  endpoint ข้างบนเท่านั้น (ห้าม hardcode)

### 4. สิ่งที่ห้ามกระทบ (ของเดิมที่ระบบอื่นพึ่งอยู่)
- รูปแบบ `record_id` เดิม: `<STATION>-<epochMs ของเวลาชั่ง>` เช่น
  `MOBILE01-1785920883555` — **ห้ามเปลี่ยน** (ใช้จับคู่ Pi ↔ .39 ทั้งระบบ)
- `code` ยังเก็บเลข lot เหมือนเดิม
- คอลัมน์ `etc` บน .39 ถูกใช้โดยระบบ QC (`'VOID'` = แถวที่ mark "ไม่เอา") — ห้ามเขียนทับ
- ธง `sent` ฝั่ง Pi: **mark sent เฉพาะเมื่อ INSERT .39 สำเร็จจริงเท่านั้น**
  (เคยมีเหตุการณ์ 2026-08-05 ที่ writer อัปเดตแล้ว INSERT ล้มช่วงหนึ่ง —
  คิวช่วยกู้กลับมาได้เพราะ sent=0 ตามจริง ให้คงพฤติกรรมนี้ไว้)
- เวลาใน `date` บน .39 เป็น **เวลาโรงงาน (+07) แบบ naive** เหมือนเดิม

### 5. ตรวจรับ
ชั่งทดสอบ 1 ครั้งแล้วเช็คจากฝั่ง SOI8:
```
GET http://172.23.10.34:18020/weighlog/station/<STATION>?src=mssql&limit=1
```
แถวล่าสุดต้องมี `"plant": "<ค่าที่ตั้ง>"` และฝั่ง Pi
(`GET .../weighlog/station/<STATION>?limit=1`) ต้องได้ค่าเดียวกันหลังฝั่ง SOI8
เปิดอ่านคอลัมน์นี้ (จะเปิดทันทีที่คอลัมน์ Pi มีจริง — แจ้งกลับมาได้เลย)

---
*คำถาม/ประสานงาน: ผ่านเจ้าของระบบ (Sittichok) — session ต้นทาง: SOI8MASTER*
